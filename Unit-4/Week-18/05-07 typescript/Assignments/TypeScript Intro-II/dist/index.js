"use strict";
let myName;
myName = "kartik";
let age;
age = 26;
let isFethcing;
isFethcing = true;
let numbers;
numbers = [1, 2, 3];
let strings;
strings = ['a', 'b', 'c'];
let tuple;
tuple = ["kartik", true];
let user = 0 /* User */;
function product(x, y) {
    return x * y;
}
;
product(2, 3);
function divide(x, y) {
    return x / y;
}
;
divide(4, 2);
function User(name) {
    console.log(name);
}
